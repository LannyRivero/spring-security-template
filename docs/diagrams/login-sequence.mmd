sequenceDiagram
    participant Client
    participant Controller as AuthController
    participant UseCase as AuthUseCase
    participant Repo as UserAccountGateway
    participant Token as NimbusJwtTokenProvider

    Client->>Controller: POST /auth/login
    Controller->>UseCase: login(credentials)
    UseCase->>Repo: findByUsername()
    Repo-->>UseCase: User
    UseCase->>Token: createAccessToken()
    Token-->>UseCase: accessToken
    UseCase->>Token: createRefreshToken()
    Token-->>UseCase: refreshToken
    UseCase-->>Controller: JwtResult
    Controller-->>Client: accessToken + refreshToken
